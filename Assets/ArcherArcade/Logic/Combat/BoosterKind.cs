namespace ArcherArcade.Logic
{
    /// <summary>Match boosters (GAME_DESIGN §5.1): optional, one of each per match, earned coins only.</summary>
    public enum BoosterKind
    {
        ShieldBubble = 0,
        MultiArrow = 1,
        IronHelmet = 2,
        ExtraHeart = 3
    }
}
