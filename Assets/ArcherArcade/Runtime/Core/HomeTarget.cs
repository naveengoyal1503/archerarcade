namespace ArcherArcade.Core
{
    /// <summary>Which menu screen the Home scene opens on (after a match, Back from a mode, first launch…).</summary>
    public enum HomeTarget
    {
        Home,
        Map,
        Modes,
        Archers,
        QuickDuelSetup,
        PvpSetup,
        TrainingSetup,
        Daily,
        Chests
    }
}
