# Archer Arcade — Progress

Rule: nothing is done until it is checked here. Update after every task.
Legend: `[ ]` todo · `[~]` in progress · `[c]` code complete: written, compiles against Unity 6000.3.24f1's DLLs
(`Tools/UnityCheck`), waits for a run in Unity / on the phone · `[x]` done and verified · `[!]` blocked (reason inline)
Target: **v1.0 "Whispering Forest"** (scope in ROADMAP.md). Each phase ends with: all tests green, an APK built,
and this file updated. The game must run after every phase.

---

## Phase 0 — Discovery and design
- [x] Project folder `apps/ArcherArcade` + `CLAUDE.md` (rules, tech, structure, conventions)
- [x] `Docs/GAME_DESIGN.md` (goals, shot, archers, arrows, AI, modes, economy, badges, audio, UI)
- [x] `Docs/LEVELS.md` (World 1 levels 1–20 in full, daily challenge, later worlds)
- [x] `Docs/ROADMAP.md` (v1.0 → v2.0, backlog, not-doing list)
- [x] `Docs/SCREEN_INVENTORY.md` (every screen/state, empty "implemented by" column)
- [x] `Docs/DESIGN_TOKENS.md` (structure; values come from the design)
- [x] Claude Design prototype: key screens (Home, Mode select, World map, Archer select, Loadout, Match HUD,
      Pass-the-phone, Victory/Defeat, Badges, Settings) in landscape, light + dark (`Design/`, 14 edge states,
      character roster with rig pivots)
- [x] Naveen approves the look (style decision: candy / NaveenCodes dark+gold / mix) — light = candy, characters 2.5D
- [x] Export the design into `Design/`, fill `DESIGN_TOKENS.md` with real values, map every screen in
      SCREEN_INVENTORY to a design frame (a few values marked *(proposed)* need Naveen's OK; 8 screens have no
      frame yet, marked "—"; *(derived)* values approved by Naveen)
- [x] Open questions answered (see "Open questions" below)

## Phase 1 — Project setup, tokens, fonts, themes
- [c] Unity 6000.3 LTS project (URP), landscape only, IL2CPP ARM64, AAB, min API 25, package
      `com.naveencodes.archerarcade`, no INTERNET permission, no Unity splash, Gamma colour space, URP mobile asset,
      Input System, TMP essentials, always-included shaders, launcher + adaptive icon — `Editor/Setup/ProjectSetup.cs`
      (menu ArcherArcade ▸ Setup ▸ Apply Project Settings; also run by `Batch.BuildAll`)
- [c] Packages: Input System 1.20.0, uGUI 2.0.0 (TMP), URP 17.3.0, Test Framework 1.6.0 — `Packages/manifest.json`;
      no analytics / web request modules listed
- [c] Folder structure + asmdefs (Logic pure C#, Runtime, Editor, Tests.EditMode, Tests.PlayMode) — Logic,
      Runtime (player + editor defines), Editor and PlayMode tests all compile in `Tools/UnityCheck`
- [x] `Rng` (PCG32, matches the reference output) + tests; `Features.cs` release switches
- [c] Fonts: Fredoka, Nunito, Material Symbols subset (`Tools/build_fonts.py`) → TMP font assets made at runtime
      (`FontLibrary`), icon font as fallback, `Icons.cs` generated
- [c] Palette light (candy) / dark (purple + gold) + element colors (`Palette`, `ButtonColors`, `ElementColors`);
      `ThemeManager` (light / dark / follow system)
- [c] `Tween` utility (pooled, unscaled by default, easings incl. OutBack), `SafeArea` — `Ease` also unit-tested
- [c] UI kit: 3D buttons (styles + pressed pose), icon buttons, switches, sliders, pills, cards, modal shell, HP bar
      with damage ghost, coin pill + counter + coin fly, star row, badge medal, toasts
- [c] Scenes `Boot`, `Home`, `Match` generated by `SceneBuilder` (ArcherArcade ▸ Build ▸ Scenes); `UIManager`
      router + Android back stack + modals + pause
- [c] `DisplayRate` (60/90/120 Hz from the display, battery saver, thermal fallback)
- [!] Verified in Unity: blocked — the cloud session has no Unity. Everything compiles against the real Unity DLLs;
      Logic tests pass with `dotnet test Tools/LogicTests/EditMode`

## Phase 2 — Shot core (Logic)
- [x] `Ballistics`: fixed-step 120 Hz flight (v0 from power, gravity, wind acceleration, arrow gravity scale)
- [x] `Wind`: per-turn roll within a level range (seeded)
- [x] `HitZones`: head / body / legs capsules per archer pose; props as boxes/circles; first-contact resolution
- [x] `Damage`: base × zone × archer level × arrow type; status effects (burn, chain, splash)
- [x] `MatchState` state machine: turns, turn timer, HP, statuses, win/lose, `ApplyShot(ShotInput)`, `Tick(dt)`
- [x] `AimSolver`: exact angle/power for a target point incl. wind and arrow type (numeric search)
- [x] EditMode tests: range/angle table, wind shifts landing as expected, determinism (same input+seed → same
      result), zone multipliers, statuses tick correctly, solver hits a target at 10–40 m in wind 0–5
      (67 tests green in `dotnet test`; re-run in Unity Test Runner once the project is opened)
- Note: props with behaviour (crates, TNT, pads…) are Phase 4; abilities are Phase 7 (`ApplyShot` rejects them
  until then)

## Phase 3 — Shot feel (Runtime)
- [c] Drag-to-aim input: start anywhere on the play area (not UI), angle −10…80°, eased power × sensitivity,
      cancel by returning within 12 dp — `Match/DragAim.cs`
- [c] Power ring + "72 % · 34°" label, drag marker, dotted trajectory preview (share from level/mode, assist,
      Ranger bonus) + ghost arc and landing ring of your last shot, haptic ticks, bow creak pitch — `HudScreen`,
      `TrajectoryPreview`
- [c] Archer view: 2.5D rig from the design roster (baked-shaded parts), idle breathing, aim + draw, release,
      hit flinch, victory hop, knockout fall — `Archers/ArcherView.cs`, `RigSolver`, `RigPoses`
- [c] Arrow view: pooled, exact replay of the Logic path, rotates with velocity, 4 trails (Classic, Sparkle,
      Rainbow, Comet), glowing element tips, sticks and quivers in props / ground / bodies, bounces, split
      children — `ArrowView`, `ArrowFlight`
- [c] Camera: frames the shooter (both archers when they fit), follows the arrow with a smooth zoom-out that fits
      shooter + arrow + target, holds on the impact, eases back; framing for 10–45 m; shake + zoom punches —
      `CameraRig`
- [c] Juice: hit-stop 70/45/30 ms, shake 8/5 dp, damage numbers, "HEADSHOT!", "DOUBLE/TRIPLE HIT!", impact
      bursts — `MatchJuice`, `TimeScaleDriver`, `ScreenShake`, `DamageNumbers`, `FxSystem`
- [c] Knockout: slow-mo 0.25× for 0.8 s real time, poof + stars, "POOF!", winner's victory pose, confetti
- [c] Wind pill with 5 bars + drifting leaves and wind streaks that show direction and strength
- [!] Verified on device (10 shots feel good, 60+ fps, no GC spikes in flight): needs Naveen's phone

## Phase 4 — Arena and props
- [c] `ArenaView` from level data: floating islands from tiles (caps, hanging rock), grass / flowers / bushes /
      trees / mushrooms kept clear of archers, 3 parallax layers + floating far islands, sky per time of day and
      theme (sun rays, moon, stars, clouds drifting with the wind)
- [x] Props behaviour in Logic (walls, crates, towers, TNT, platforms, pads, targets, barrels, shields, rope +
      cage, knockback near edges) — `Logic/Arena`, `MatchState`
- [c] Prop visuals fitted to their Logic shapes: wooden/stone walls, crate → cracked → planks, tower crates knocked
      off / toppling / settling, TNT with a sparking fuse, barrels, squashing bounce pads, targets on posts or
      balloons, swing targets on a wooden frame, apple dummy, rope with the caged fox (cage drops, fox cheers),
      shields that fall flat, floating platforms with propellers, the boss's growing vine wall — `PropView`
- [c] Target types: static, swinging, moving (follow the match clock), apple + friendly dummy
- [c] World 1 art pass: forest day / dusk / night lighting, fireflies at dusk and night, drifting leaves
- [x] Tests: each prop's behaviour in Logic (hits, breaks, bounces, explosions) — `PropTests`, 21 tests

## Phase 5 — Computer opponent
- [c] `AiPlayer`: target zone choice, solver + difficulty noise, learning per miss, think time + aim animation —
      Logic done and tested (lobs over walls, gets past shields); the visible think + aim swing is `AI/AiTurn.cs`
- [x] Profiles Easy / Medium / Hard / Boss in config (GAME_DESIGN §6.2) — `AiProfile`; σ tuned, see Decisions
- [x] Ability use rules per profile — `AiProfile` + `AiPlayer` (abilities exist since Phase 7)
- [x] Fairness tests: 1,000 simulated shots per profile, first-shot hit rates inside the §6.3 ranges
      (Easy ~20 %, Medium ~40 %, Hard ~65 %, Boss ~77 %)
- [c] AI-vs-AI: 20 matches finish without hangs, both sides win sometimes — Logic test `AiTests` passes; the
      PlayMode version (`MatchPlayTests`, autopilot vs computer to the result screen) compiles, needs Unity

## Phase 6 — Match flow and HUD
- [c] Match HUD (design `match`): HP panels + names + portraits (hurt face when low) with a gold ring on the
      active side, turn timer ring (orange ≤ 6 s, red + pulse ≤ 3 s), wind pill, ability button (element colour,
      charge pips, pulses when ready), arrow-tip slots with ammo, pause, turn label ("Your turn" / "Moss is
      aiming…"), goal / wave / round chip, hits + headshots counters, status icons, Gauntlet dots — `HudScreen`
- [c] Turn system: coin-flip banner (duels), timer ticks + timeout toast, status ticks between turns with their
      effects, next Gauntlet archer walks in — `MatchSceneRoot`
- [c] Pause modal (resume, restart, quit, sound, settings) with timeScale 0; Android Back opens it — `PauseModal`
- [c] Victory screen: stars pop one by one, coins fly into the counter, first-clear bonus, badge toasts, next /
      replay / home, chest button — `ResultScreen`
- [c] Defeat screen: tip after 2 losses, retry, "Retry with Assist" after 3, map
- [c] Level intro banner (name, world, goal) and "New!" idea cards before the first turn — `MatchBanner`,
      `IdeaCardModal`

## Phase 7 — Archers, abilities, arrow tips, injury looks, enemies
- [c] Archer data: stats, passive, ability, unlock rule, upgrade curve, skins — `ArcherDef`/`ArcherTable`,
      `EconomyConfig` upgrade costs, `CosmeticCatalog` skins; tuning in the `GameConfig` ScriptableObject
- [x] Ranger: passive (longer preview) + Triple Shot (Logic; the preview itself is Runtime)
- [x] Fire Archer: burn passive + Meteor Arrow (splash + burn)
- [x] Electric Archer: chain passive + Storm Bolt (ignores wind, chains twice)
- [x] Bomb Archer: explosive passive + Cluster Bomb (3 bomblets at apex)
- [c] Ability charge (3 turns, 2 after a headshot), button states (pips, pulse when ready, armed ring), cast
      ring + flash + sparks + ability name + SFX per ability — Logic tested; HUD + `MatchJuice`
- [c] 8 arrow tips (GAME_DESIGN §5): Normal, Fire, Electric, Split, Bomb, Heavy, Ice, Poison — glow in flight,
      unlock by level (Fire L3 … Poison L18), ammo per match, arrow-tip slots in the HUD — Logic tested; locked tips
      with their unlock level show in the Loadout (the HUD shows the tips you took)
- [c] Impact effects (§3.3.1): fireball + burning body, **lightning bolt from the sky** (0.15 s later, screen
      flash, shake) + stun + chain sparks + bubble pop, bomb shockwave + smoke + debris + knockback + slow-mo 0.1 s,
      ice shards + encase + slow draw, poison cloud + bubbles — each with its sound, haptic and camera move; pooled;
      scaled by "Reduce motion"
- [x] Archer + own element bonus (+20 %)
- [c] **Injury looks** (§3.3.2): 4 HP stages (band-aid → limp + sweat → dizzy stars + bandage) + knockout poof;
      element marks (soot, sparks, frost, green tint, bruise); stuck arrows stay visible until the turn ends —
      Logic tested; looks in `ArcherView` driven by the HP shown on screen
- [c] Enemy types (§4.1): Bandit Archer, Crossbow Scout, Shield Bearer, Twin Shooter, Healer Druid, Tower
      Sniper, bubble-shield caster — behaviour tested (`EnemyTable`); looks drawn in the design's style
      (`Tools/art/characters_ext.js`), heal / bubble / enrage / second-shot effects in `MatchJuice`
- [c] Match boosters (§5.1): Shield Bubble, Multi Arrow, Iron Helmet, Extra Heart (coins only, never required) —
      Logic tested, prices in `EconomyConfig`, bought in the Loadout, shown as HUD status icons
- [c] Loadout screen: archer, skin, trail, up to 3 special tips, boosters — `LoadoutRules` + `LoadoutScreen`
- [x] Tests: every ability's and tip's damage/area/status math, ammo limits, charge timing, injury stage per HP,
      boosters apply once, every level winnable without boosters (`WinnabilityTests`)

## Phase 8 — Campaign World 1
- [x] Level data for levels 1–20 exactly as `LEVELS.md` (goals, opponents, distances, wind, props, par, rewards)
      — `Logic/Campaign/WorldOne.cs`, checked row by row by `CampaignTests`
- [x] Goal types: Duel, Targets, Apple Shot, Gauntlet (HP carries), Rescue, Trick Shot, Boss — `LevelRun`
- [x] Stars + par rules (GAME_DESIGN §7.2), best stars/turns saved — `StarRules`, `Profile.RecordLevel` (tested)
- [x] Mini-boss Captain Thorn (Thorn Volley) and boss Forest Warden (rotating shields, Vine Wall, Rain of
      Leaves, Enrage, weak-spot knot) — Logic + `BossTests`; looks are Runtime
- [c] World map screen: path of 20 nodes, stars under nodes, chest nodes after 5/10/15/20, locked nodes shake —
      `MapScreen`
- [c] Tutorial (level 1) with an animated hand showing the drag, step bubbles, Skip; "New!" cards once each (flags
      saved) — `Tutorial/TutorialOverlay`, `IdeaCardModal`
- [c] Losing flow: tips after 2 losses, Assist after 3 (no ★★★ with assist) — rules tested; `ResultScreen` shows
      the tip and "Retry with Assist"
- [x] Level validation tests: every level solvable by the AI solver, rewards/unlocks match the table —
      plus a perfect-aim bot with normal progression and no boosters wins every level (≥ 7/8 seeds)

## Phase 9 — 2-Player (same phone)
- [c] Setup screen: player names (default P1 / P2), archer + skin each, arena, best of 1/3/5, wind on/off,
      preview length, handicap HP 70–130 % — `PvpSetupScreen`
- [c] Pass-and-play: "Pass the phone" cover hides the board between turns; "I'm ready" (or a tap) starts the turn;
      your own last shot shows as a ghost arc, the other player's never — `PassModal`
- [c] Round flow + scoreboard, rematch, swap sides — `PvpSeries` (tested) + round / final `ResultScreen`
- [x] Rewards: badge progress only (no coins) — `Rewards.TwoPlayer`, `Profile.RecordMatchEnd`
- [c] PlayMode test: 2-player shows the pass cover between turns and hands over to the other side
      (`MatchPlayTests`); the series flow is covered by `ModesTests`

## Phase 10 — Other modes
- [c] Quick Duel: archer + difficulty + arena pickers, coin rewards (config) — `QuickDuel`, `ArenaCatalog`,
      `Rewards.QuickDuel`, `LoadoutScreen.QuickDuel`
- [c] Daily Challenge: date seed → template + twist, same level on every phone (test), streak, 7-day chest —
      Logic + tests (a year of dailies solvable), `DailyScreen`, result records the streak
- [c] Training Range: 10/20/30/40 m targets, wind slider, long preview, no timer, hit + bullseye stats on the HUD
      — `TrainingRange`, `LoadoutScreen.Training`
- [c] Survival (pulled forward from ROADMAP 1.1, in the design's mode list): endless waves, HP carries over (+20),
      coins per wave (capped), best wave on Stats — `SurvivalRun` (tested), `Rewards.Survival`

## Phase 11 — Meta: economy, upgrades, chests, badges, stats
- [x] `EconomyConfig` (all numbers from GAME_DESIGN §8); Wallet never negative (test); wrapped by the `GameConfig`
      ScriptableObject (`ConfigBuilder` creates `Resources/GameConfig.asset`)
- [x] Archer upgrades lvl 1–10 (cost curve, +4 % dmg / +4 HP per level), unlock rules (L5, 20 stars, boss)
- [c] Chests after levels 5/10/15/20: seeded contents, shake + open animation, coin fly — rules tested,
      `ChestsScreen` + `ChestOpenModal`
- [c] 22 badges × bronze/silver/gold (GAME_DESIGN §9), progress tracking, unlock toast, badge wall, pin 3 on Home
      — catalog, tiers, rewards, pins, `StatsRecorder` tested; `BadgesScreen`, `BadgeModal`, result toasts
- [c] Stats screen: shots, accuracy, headshots, longest shot, wins/losses per mode, favourite archer, time played
      — tracked in the save (tested), `StatsScreen`
- [c] Shop (coins only): skins and trails, preview before buying — rules tested, `ShopScreen` + `ShopPreviewModal`

## Phase 12 — Screens and full UI polish
- [c] Every screen in SCREEN_INVENTORY built from the design (light + dark), with open/close animations (aaIn fade +
      scale, aaPop dialogs, staggered pops) — see the "Implemented by" column in SCREEN_INVENTORY.md
- [c] Home: profile card (pinned badges), big Play (continues campaign), mode cards, archer idling on a stage
- [c] Settings: music, sound, haptics, theme (auto / light / dark), language (EN / Hinglish), left-handed, bigger
      targets, aim sensitivity, assist, reduce motion, battery saver, colour-blind, name, How to play, Credits,
      Privacy; footer **"In loving memory of Maa ❤️"**
- [c] Credits screen with the Maa tribute
- [c] Every button has a real action; Android Back correct everywhere (PlayMode test walks every Home target
      and Back; match: Back pauses, pass cover: Back pauses, result: Back leaves)
- [c] Capture tool (`Batch.Capture`, menu ArcherArcade ▸ Capture ▸ Screens + Match) writes every screen in light
      and dark plus three matches to Builds/Capture/ for comparison with the design

## Phase 13 — Audio and haptics
- [c] `AudioManager`: SFX pool, variations + pitch jitter, music crossfade, ducking (pause, stingers), bow creak
      and fire crackle loops
- [x] `Tools/gen_sounds.py`: placeholder for every sound id in DESIGN_TOKENS §8 (55 ids, 79 files incl. variations);
      Final/ overrides Generated/
- [x] Music: Home, World 1, Boss, Duel loops (seamless, whole bars), victory + defeat stingers
- [c] `HapticsManager`: patterns from GAME_DESIGN §11, respects the setting
- [x] Test: every sound id has a clip and matches DESIGN_TOKENS — `AudioFilesTests`

## Phase 14 — Accessibility, language, performance
- [x] Hinglish strings table (MindTap approach) + tests: both languages, same placeholders, every key the Runtime
      uses exists — `StringsTests`
- [c] Colour-blind HP colours + element icons always shown with their colour; reduce motion (less shake, no
      slow-mo, fewer particles, no pops); bigger targets (HUD ×1.15); left-handed HUD mirror
- [!] Performance: profile a busy match on a mid-range phone — needs Naveen's phone (pools are in place: arrows,
      480 particles, damage numbers, confetti; no allocations in the flight loop)
- [!] Thermal fallback + battery saver verified on the phone — needs Naveen's phone

## Phase 15 — Quality and release
- [x] EditMode suite green in .NET: ballistics, solver, AI fairness, damage, statuses, props, levels 1–20, daily
      seed, economy, badges, save/load + migrations, strings, audio (248 tests); re-run in Unity's Test Runner
- [c] PlayMode suite: every mode starts a match, Quick Duel on autopilot reaches the result, 2-player shows the
      pass cover between turns, pause freezes time and Back resumes, every Home screen opens and Back returns,
      Back on Home asks before leaving — `Tests/PlayMode` (compiles; needs Unity to run)
- [!] Flow walk on the phone: first launch → tutorial → win → lose → retry → chest → unlock Fire Archer →
      upgrade → 2-Player match → daily challenge → settings toggles → relaunch keeps everything — Naveen's phone
- [~] Store assets: icon 512 + adaptive icon + feature graphic 1024×500 (`Tools/art/make_store_art.py`), short +
      full description, release notes, rating and data-safety answers (`Docs/Store/STORE_LISTING.md`); the 8
      landscape screenshots come from `ArcherArcade ▸ Capture ▸ Screens + Match` on Naveen's PC
- [~] Privacy policy text (`Docs/PRIVACY_POLICY.md`, same words as the in-game Privacy screen); the website page
      naveencodes.com/archer-arcade/ waits for Naveen's site setup
- [ ] AAB signed with a new upload key (keep the keystore safe, never regenerate), verified with bundletool
      (version, signer, no INTERNET) — only when Naveen asks
- [~] Final report: what's built, deviations, what Naveen must provide — see the latest summary in the chat and
      the Decisions log below

---

## Open questions (answered by Naveen 2026-09-25)
1. Style → **light theme candy colors**. Dark theme stays the prototype's purple + gold (not asked to change).
2. Art → **2.5D** (3D-looking characters).
3. Art/music → **placeholder now** (generated), replaced later by id.
4. Name "Archer Arcade" + package `com.naveencodes.archerarcade` → **final**.
5. Derived token values (glow colors, bigger targets 56 dp, camera 0.35 / 0.5 s) and the Bomb splash rule → **OK**.

## Decisions log
| Date | Decision |
|---|---|
| 2026-09-25 | Project created at `apps/ArcherArcade`. Engine Unity 6000.3 LTS (same install and workflow as MindTap). |
| 2026-09-25 | **Landscape** orientation (duel reads best side-by-side). Reference 844 × 390. Confirmed by Naveen (2026-09-25). |
| 2026-09-25 | v1.0 scope kept small (1 world, 4 archers, 4 arrows, local 2-player); the rest staged in ROADMAP 1.1–1.3. |
| 2026-09-25 | Multiplayer v1 = same-phone pass-and-play. Online (2.0) later; match logic is input-driven and deterministic so it can be added without rewrites. No INTERNET permission until then. |
| 2026-09-25 | 2-Player gives badge progress only, no coins (can't be farmed). |
| 2026-09-25 | Special arrows refill every match (no permanent consumables → nothing to "run out of" or buy). |
| 2026-09-25 | Design prototype vs GAME_DESIGN conflicts → **GAME_DESIGN wins** for 1.0: prototype shows 10 archers bought with coins (800–3600), 10 arrow types (Piercing, Homing, Grapple, Healing…), boss "Old Barkjaw", Electric HP 90, Cluster 4 bomblets, a Missions screen and "/63 stars". 1.0 keeps 4 archers unlocked by play, the 8 tips of §5, the Forest Warden, 3 bomblets, Electric 100 HP; Missions stay in 1.1. The prototype is used for look only. |
| 2026-09-25 | Logic uses `double` + own `DetMath` trig (polynomial, only +−×÷ and sqrt) so shots are bit-identical on every device (needed for 2.0 online). |
| 2026-09-25 | Tuning lives in plain Logic config classes (`ShotConfig`, `DamageConfig`, `TipTable`, `MatchConfig`, `BodyConfig`) with the GAME_DESIGN defaults; Runtime ScriptableObjects will fill them. |
| 2026-09-25 | Tip damage assumes the Ranger (25): damage = tip × (archer dmg / 25) × level × zone × element bonus. Bomb splash hits everyone near the impact **except** the arrow's direct target (direct hit = 20, near miss / bystander = 12); Bomb Archer passive splash adds to the tip's splash. Burn/poison refresh (keep the stronger), never stack. A stunned turn is never shorter than 4 s. |
| 2026-09-25 | Naveen: light theme = candy; characters **2.5D**; placeholder art + audio for now; name + package final; derived tokens and Bomb splash rule approved. |
| 2026-09-25 | 2.5D means: low-poly 3D character meshes with toon shading + outline (URP), rigged with the Design roster's bone names (hood, head, torso, cape, upper_arm_front…); injury looks = extra mesh parts + material swaps; gameplay stays on the 2D x/y plane (Logic unchanged); side camera with 3D lighting and parallax layers. Until final art, placeholder rigs built from primitives with the same bones. The roster SVGs are the look/proportion reference. |
| 2026-09-25 | Phases 4–5 Logic done before Phase 3 (Runtime shot feel), because Phase 3 needs Unity and the cloud session has none. Phase 3 is next on Naveen's PC. |
| 2026-09-25 | AI noise σ tuned to meet GAME_DESIGN §6.3 (first values made every profile ~20 points too accurate): Easy 12°/20 %, Medium 5.5°/9.5 %, Hard 2.8°/5 %, Boss 1.8°/3.5 %. GAME_DESIGN §6.2 updated. |
| 2026-09-25 | Props rules: a tower crate is knocked off by any single hit (standalone crates need 2); any explosion touching a tower topples it and sets off its TNT; explosions (TNT, barrels) hurt every archer in range, the shooter too; a knocked-down shield is back up when its owner's second turn after the knock starts (the shooter gets one free shot); arrows stop at ropes; Bomb knockback 0.8 m, never pushes off an island (stops 0.4 m from the edge and stumbles). |
| 2026-09-25 | Moving targets follow a match clock that only advances with aiming time (`Tick`) and flight time, so release timing matters but stays deterministic. Replays / online must record the release clock (`ShotResult.Clock`) with each `ShotInput`. |
| 2026-09-25 | Ability rules: Triple Shot fans the picked tip (one ammo, 60 % per arrow); Meteor, Storm Bolt and Cluster Bomb fire their own arrow (no ammo) whose damage is final (no archer-base scaling, no element bonus; upgrade level still scales it). Storm Bolt flies 25 % faster and ignores wind. Cluster bomblets split ±8°. The turn an ability is used does not count toward the next charge. Multi Arrow booster = Triple Shot fan on the first shot (60 %); it never stacks with Triple Shot. |
| 2026-09-25 | Enemy rules: Twin Shooter gets 2 aimed shots per turn (timer resets for the 2nd); Healer Druid heals 10 at the start of every 2nd own turn (never above max); bubble casters cast at the start of every 3rd own turn; a bubble eats one arrow (no damage), Electric pops it and still hits; Iron Helmet turns the first headshot into a body hit (no headshot credit). Default enemy HP: Scout 70, Twin 80, Druid 90, Sniper 90, Bramble 120 (levels may override). |
| 2026-09-25 | Campaign rules: duel stars = 1 for a win + 1 for ≥ 50 % HP + 1 for ≤ par turns (each condition adds a star); target-type levels allow 2 × par + 2 arrows; hitting a dummy fails the Apple Shot; cutting the rope (or knocking Moss out) wins the Rescue. Thorn Volley = 5 arrows ±3° at 40 %. Warden: body scale 1.35, shields rotate every turn through 3 openings (legs → head → body + knot), vine wall 2.6 m tall 3.5 m in front of the player every 3rd Warden turn (2 hits, fire burns it at once), Rain of Leaves = 3 arrows of 12 falling from 14 m, Enrage at ≤ 30 % HP. |
| 2026-09-25 | Level fixes found by the validation tests: L10's TNT tower is 3 crates (5 hid Captain Thorn's body in a headwind); L12's roof/pad were moved so the hidden targets are reachable only by a bounce. With normal progression (one upgrade per 5 levels, tips unlocked so far) a perfect-aim player wins every level on ≥ 7/8 seeds; a level-1 Ranger with Normal arrows only wins L19/L20 on 5/8 — watch these two in playtests. |
| 2026-09-25 | Meta rules: cosmetics = 2 skins per archer (default + Forest Cloak 300 / Ember Coat 450 / Neon Bolt from Thunderstruck silver / Blast Goggles 600) and trails Classic, Sparkle 200, Comet 400, Rainbow (7-day daily streak). Chest cosmetic chance 35 %, never something already owned. Campaign losses pay no coins (Quick Duel losses pay 3). Warden Slayer gold = 3★ on the boss. Daily twists: Strong Wind 3–5, Split only / Heavy only (unlimited, no Normal), One arrow at 35 m, Tiny targets, Night. Training board 0.6 m radius, bullseye within ±0.2 m of centre. Quick Duel opponent = a random hero (level 1/2/3 by difficulty). |
| 2026-09-25 | Save = versioned JSON written by our own tiny JSON code in Logic (tested), read forgivingly: missing fields get defaults, bad values are clamped, garbage gives a fresh save, unknown fields are ignored. |
| 2026-09-25 | Unity compile check: `Tools/UnityCheck` compiles Runtime/Editor code (and uGUI, TextMeshPro, Input System 1.20.0) against Unity 6000.3.24f1's DLLs (`fetch_unity_refs.sh`), so cloud sessions catch compile errors before Naveen opens the project. |
| 2026-09-25 | Cloud sessions have no Unity: Logic is compiled and tested with .NET 8 (`Tools/LogicTests`, same NUnit API). APKs and Unity-side checks happen on Naveen's PC. |
| 2026-09-25 | **2.5D look, revised:** instead of low-poly meshes, the Design roster's SVG parts are rendered and given baked "vinyl toy" volume shading (highlights, rim light, ambient occlusion, depth-tinted back limbs, soft ground shadow) by `Tools/art` and assembled every frame by a port of the design's rig (`RigSolver`). Same bones and proportions as the roster, matches the prototype exactly, animates reliably on mid-range phones, and final art can replace parts by id. GAME_DESIGN §12 updated. |
| 2026-09-25 | Extra enemy looks (Bandit, Crossbow Scout, Shield Bearer, Healer Druid, Tower Sniper, Twig twins) and the 4 earned skins were drawn in the roster's style (same kit, outline, shading) where the design had none. |
| 2026-09-25 | **Survival** pulled forward from ROADMAP 1.1 because the design's Home / Modes screens show it: endless waves of World 1 archers, HP carries over (+20), AI grows Easy → Boss. Coins: 10 per wave cleared, capped at 200 per run, + headshot coins (`EconomyConfig.SurvivalCoinsPerWave / Cap`). |
| 2026-09-25 | Colour space **Gamma** (like MindTap) so the HUD's translucent white panels and the design's rgba() colours look the same as the CSS prototype. URP mobile asset: no HDR, no MSAA, no shadows, no depth/opaque textures (2D sprites only). |
| 2026-09-25 | Match presentation replays Logic exactly: a shot is resolved at release, then arrows are drawn from their `ArrowPath`s and every event fires at its own time; between-turn events (burn, heal, platforms, shields, vines, wind) play after a 0.7 s hold. The flying arrow starts on the bow's drawn arrow and blends onto the Logic path in 0.15 s. The HP bars drop when the replay reaches each hit. |
| 2026-09-25 | Theme picker has Auto / Light / Dark (design has Light / Dark; Auto follows the phone). The Chests screen shows all 4 World 1 chests. Home's 4th mode card is Survival as in the design. |
| 2026-09-25 | "Bigger targets" (GAME_DESIGN §13 "bigger touch targets") scales the match HUD 1.15× and the menus' touch targets; it never changes hit zones. Left-handed mirrors the HUD's bottom bar only. |
| 2026-09-25 | Placeholder audio is synthesised by `Tools/gen_sounds.py` (original, deterministic, 22 kHz mono): 55 ids, 3 variations for impacts, seamless 8-bar music loops. Real files with the same id in `Resources/Audio/Final/` replace them. |
| 2026-09-25 | PlayMode tests can drive a match: `MatchSceneRoot.Autopilot` plays the human side with a Hard AI, `ShootNow` fires a scripted shot, `TimeScaleDriver.Speed` runs matches faster. |
